<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<!--
  Main application layout for CookYourBooks.

  The sidebar (left) contains navigation buttons. The content area (center)
  is a StackPane that gets swapped by MainViewController when the user
  navigates between features.
-->
<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="app.cookyourbooks.gui.view.MainViewController"
            stylesheets="@../css/cookyourbooks.css"
            styleClass="root-pane">

    <!-- Sidebar navigation -->
    <left>
        <VBox fx:id="sidebar" styleClass="sidebar" spacing="4">
            <padding>
                <Insets top="16" right="8" bottom="16" left="8"/>
            </padding>

            <Label text="CookYourBooks" styleClass="sidebar-title"/>

            <Button fx:id="libraryButton" text="Library" styleClass="nav-button" maxWidth="Infinity"/>
            <Button fx:id="editorButton" text="Recipe Editor" styleClass="nav-button" maxWidth="Infinity"/>
            <Button fx:id="importButton" text="Import" styleClass="nav-button" maxWidth="Infinity"/>
            <Button fx:id="searchButton" text="Search" styleClass="nav-button" maxWidth="Infinity"/>
        </VBox>
    </left>

    <!-- Content area — feature views are swapped in here -->
    <center>
        <StackPane fx:id="contentArea" styleClass="content-area">
            <padding>
                <Insets top="16" right="16" bottom="16" left="16"/>
            </padding>
        </StackPane>
    </center>

</BorderPane>
