/**
 * FXML controllers for CookYourBooks views.
 *
 * <p>Note: This package is intentionally NOT annotated with {@code @NullMarked} because FXML
 * controllers use field injection ({@code @FXML} fields are initialized by the FXML loader, not the
 * constructor), which is incompatible with NullAway's strict initialization checking.
 */
package app.cookyourbooks.gui.view;
